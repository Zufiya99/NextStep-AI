/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'elearningindustry.com', 
      'randomuser.me',
      'media.licdn.com'
    ],
  },
}

module.exports = nextConfig